# Copia este archivo como .env (en la raíz del proyecto) y completa tus valores.
# .env NO se sube al repo (está en .gitignore).

SECRET_KEY=pon-aqui-una-clave-secreta-cualquiera-para-desarrollo

DB_NAME=agendamientoCitas
DB_USER=postgres
DB_PASSWORD=tu_password
DB_HOST=localhost
DB_PORT=5432
